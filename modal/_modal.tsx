import React from "react";
import { Modal, Button, Text } from "@nextui-org/react";

interface Props{
    isVisible: boolean,
}
export const ModalContent: React.FC<Props> = ({isVisible}) =>{
   
    return(
        
    )
}
